<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Frontend.Components.Register.PasswordStep">
    
    <ScrollView>
        <VerticalStackLayout Spacing="15" Padding="0,0,0,20">
            <Label 
                Text="Create a password"
                FontSize="18"
                TextColor="White"
                Margin="0,10,0,5"/>
                
            <Label
                Text="Make it strong and secure"
                FontSize="14"
                TextColor="LightGray" />
                
            <!-- Password Entry with Toggle -->
            <Grid ColumnDefinitions="*" Margin="0,10,0,0">
                <Entry 
                    x:Name="PasswordEntry"
                    Placeholder="Password"
                    PlaceholderColor="#7f7f7f"
                    BackgroundColor="#2a2a2a"
                    TextColor="White"
                    IsPassword="true"
                    TextChanged="OnPasswordChanged"/>
                
                <Button 
                    x:Name="PasswordToggle"
                    BackgroundColor="Transparent"
                    BorderColor="Transparent"
                    HorizontalOptions="End"
                    VerticalOptions="Center"
                    Margin="0,0,5,0"
                    Padding="0"
                    WidthRequest="48"
                    HeightRequest="48"
                    Clicked="OnPasswordToggleClicked">
                    <Button.ImageSource>
                        <FontImageSource
                            FontFamily="MaterialIcons"
                            Glyph="&#xe8f4;"
                            Color="#7f7f7f"
                            Size="24" />
                    </Button.ImageSource>
                </Button>
            </Grid>
            
            <!-- Confirm Password Entry with Toggle -->
            <Grid ColumnDefinitions="*" Margin="0,5,0,0">
                <Entry 
                    x:Name="ConfirmPasswordEntry"
                    Placeholder="Confirm password"
                    PlaceholderColor="#7f7f7f"
                    BackgroundColor="#2a2a2a"
                    TextColor="White"
                    IsPassword="true"
                    TextChanged="OnConfirmPasswordChanged"/>
                
                <Button 
                    x:Name="ConfirmToggle"
                    BackgroundColor="Transparent"
                    BorderColor="Transparent"
                    HorizontalOptions="End"
                    VerticalOptions="Center"
                    Margin="0,0,5,0"
                    Padding="0"
                    WidthRequest="48"
                    HeightRequest="48"
                    Clicked="OnConfirmToggleClicked">
                    <Button.ImageSource>
                        <FontImageSource
                            FontFamily="MaterialIcons"
                            Glyph="&#xe8f4;"
                            Color="#7f7f7f"
                            Size="24" />
                    </Button.ImageSource>
                </Button>
            </Grid>
            
            <!-- Password Requirements -->
            <Frame BackgroundColor="#1a1a1a" BorderColor="#333333" Padding="15" Margin="0,10,0,0">
                <VerticalStackLayout Spacing="10">
                    <Label Text="Password requirements:" TextColor="White" FontSize="14" Margin="0,0,0,5" />
                    
                    <HorizontalStackLayout Spacing="10">
                        <BoxView x:Name="LengthIndicator" 
                                WidthRequest="15" HeightRequest="15" 
                                BackgroundColor="#333333" 
                                CornerRadius="7.5"
                                VerticalOptions="Center" />
                        <Label Text="8-16 characters" 
                            TextColor="LightGray" FontSize="13"
                            VerticalOptions="Center" />
                    </HorizontalStackLayout>
                    
                    <HorizontalStackLayout Spacing="10">
                        <BoxView x:Name="UppercaseIndicator"
                                WidthRequest="15" HeightRequest="15" 
                                BackgroundColor="#333333" 
                                CornerRadius="7.5"
                                VerticalOptions="Center" />
                        <Label Text="At least one uppercase letter" 
                            TextColor="LightGray" FontSize="13"
                            VerticalOptions="Center" />
                    </HorizontalStackLayout>
                    
                    <HorizontalStackLayout Spacing="10">
                        <BoxView x:Name="LowercaseIndicator"
                                WidthRequest="15" HeightRequest="15" 
                                BackgroundColor="#333333" 
                                CornerRadius="7.5"
                                VerticalOptions="Center" />
                        <Label Text="At least one lowercase letter" 
                            TextColor="LightGray" FontSize="13"
                            VerticalOptions="Center" />
                    </HorizontalStackLayout>
                    
                    <HorizontalStackLayout Spacing="10">
                        <BoxView x:Name="DigitIndicator"
                                WidthRequest="15" HeightRequest="15" 
                                BackgroundColor="#333333" 
                                CornerRadius="7.5"
                                VerticalOptions="Center" />
                        <Label Text="At least one number" 
                            TextColor="LightGray" FontSize="13"
                            VerticalOptions="Center" />
                    </HorizontalStackLayout>
                    
                    <HorizontalStackLayout Spacing="10">
                        <BoxView x:Name="SpecialCharIndicator"
                                WidthRequest="15" HeightRequest="15" 
                                BackgroundColor="#333333" 
                                CornerRadius="7.5"
                                VerticalOptions="Center" />
                        <Label Text="At least one special character" 
                            TextColor="LightGray" FontSize="13"
                            VerticalOptions="Center" />
                    </HorizontalStackLayout>
                    
                    <HorizontalStackLayout Spacing="10">
                        <BoxView x:Name="MatchIndicator"
                                WidthRequest="15" HeightRequest="15" 
                                BackgroundColor="#333333" 
                                CornerRadius="7.5"
                                VerticalOptions="Center" />
                        <Label Text="Passwords match" 
                            TextColor="LightGray" FontSize="13"
                            VerticalOptions="Center" />
                    </HorizontalStackLayout>
                </VerticalStackLayout>
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</ContentView>